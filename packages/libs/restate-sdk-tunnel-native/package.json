{
  "name": "@restatedev/restate-sdk-tunnel-native",
  "version": "0.0.0",
  "private": true,
  "description": "Embedded native relay-tunnel receiver (napi-rs) for the Restate TypeScript SDK",
  "author": "Restate Developers",
  "email": "code@restate.dev",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.cts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "index.cjs",
    "index.d.cts",
    "restate-sdk-tunnel-native.*.node",
    "README.md"
  ],
  "napi": {
    "name": "restate-sdk-tunnel-native"
  },
  "//scripts": "This package needs a native `napi build` step that the shared CI pipeline does not run yet, so it deliberately does NOT define the turbo task scripts (_build / _check:types / _test / lint). That opts it out of the repo-wide `turbo run` (build/verify) until the prebuild matrix + native CI land — see development/relay-receiver-napi-plan.md. Build it manually with the scripts below (build:native first, then build:wrapper).",
  "scripts": {
    "build:native": "napi build --platform --release --js index.cjs --dts index.d.cts",
    "build:native:debug": "napi build --platform --js index.cjs --dts index.d.cts",
    "build:wrapper": "tsc --noEmit --project tsconfig.build.json && tsdown",
    "clean": "rm -rf dist *.tsbuildinfo .turbo"
  },
  "dependencies": {},
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4",
    "@restatedev/restate-sdk": "workspace:*"
  },
  "peerDependencies": {
    "@restatedev/restate-sdk": "^1.15.0"
  }
}
