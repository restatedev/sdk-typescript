# Rust build output
/target
Cargo.lock

# napi build artifacts (prebuilt binaries are published as per-platform packages, not committed)
*.node

# per-platform npm package dirs generated by `napi create-npm-dirs`
/npm

# prebuilt binaries downloaded during release (see .tools/publish-native.sh)
/artifacts
