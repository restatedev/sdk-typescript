{
  "name": "@restatedev/restate-sdk",
  "version": "1.0.11",
  "description": "Typescript SDK for Restate",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/restatedev/sdk-typescript.git"
  },
  "release-it": {
    "github": {
      "release": true
    },
    "npm": {
      "publish": false
    }
  },
  "publishConfig": {
    "@restatedev:registry": "https://npm.pkg.github.com"
  },
  "type": "commonjs",
  "main": "dist/public_api.js",
  "types": "dist/public_api.d.ts",
  "author": "Restate Developers",
  "license": "BUSL",
  "email": "code@restate.dev",
  "scripts": {
    "proto": "npx buf generate",
    "build": "tsc --noEmitOnError && tsc --p ./examples/ --noEmit",
    "test": "jest",
    "lint": "eslint --ignore-path .eslintignore --ext .ts .",
    "format": "prettier --ignore-path .eslintignore --write \"**/*.+(js|ts|json)\"",
    "example": "ts-node-dev --respawn --transpile-only ./examples/example.ts",
    "lambda-example": "ts-node-dev --respawn --transpile-only ./examples/lambda_example.ts",
    "verify": "npm run test && npm run lint && npm run build",
    "release": "release-it"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "protobufjs": "^7.2.2",
    "ts-proto": "^1.140.0"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.15.0",
    "@types/jest": "^29.4.0",
    "@types/node": "^18.14.2",
    "@typescript-eslint/eslint-plugin": "^5.53.0",
    "@typescript-eslint/parser": "^5.53.0",
    "@types/aws-lambda": "^8.10.115",
    "eslint": "^8.35.0",
    "prettier": "^2.8.4",
    "protobufjs-cli": "^1.1.1",
    "ts-jest": "^29.0.5",
    "ts-node-dev": "^1.1.1",
    "typescript": "^4.9.5"
  }
}
